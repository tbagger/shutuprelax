<body>
        <!--[if lt IE 9]>
            <p class="alert alert-danger m-top">You are using an <strong>outdated</strong> browser. Please <a href="http://browsehappy.com/">upgrade your browser</a> to improve your experience.</p>
        <![endif]-->

<div class="wrapper">

  <%= render 'layouts/navigation' %>

  <div class="main">
    <div class="container">
      <div class="page-title">
        <h1>massage menu</h1>
      </div>
    </div>
    <div class="container work-grid">
      <div class="row">
        <div class="col-lg-12 col-md-11 col-sm-12">
          <% @massages.each do |m| %>
          <div class="work-item">
            <a class="work-image" href='<%= massage_path(m.id) %>'>
              <span class="btn-arrow"><i class="fa fa-angle-right"></i></span>
              <%= image_tag( m.thumbnail, :alt=>"" ) %>
            </a>
            <div class="workCaption">
              <%= link_to massage_path(m) do %>
                <h5><%= m.title %></h5>
              <% end %>
            </div>
          </div> <!-- /.works-item -->
          <% end %>
        </div>
      </div>

    </div><!-- /.works-grid -->
  </div>

  <div class="push"></div>
</div>
